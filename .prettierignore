.next
node_modules
drizzle
package-lock.json
.claude/skills
.claude/agents
# Prose docs (specs, plans, backlogs) — authored by hand / by agents, not held
# to Prettier's markdown reflow.
docs
# Xcode-owned asset catalog JSON — format is dictated by Xcode, not Prettier.
ios/SyncFit/SyncFit/Assets.xcassets
# Vendored design-system source (from the Claude Design handoff bundle) — kept
# faithful to the upstream bundle, not reformatted to our Prettier style.
src/styles/design
